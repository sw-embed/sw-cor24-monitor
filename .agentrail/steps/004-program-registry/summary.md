Implemented program registry with parallel arrays (prog_names, prog_entries, prog_flags, prog_count) for up to 16 programs. Added mon_strcmp string helper, mon_register, mon_find_program, mon_run_by_name, and mon_list_programs. Updated main() to register echo/ret42/exit7 and exercise all registry operations. All tests pass: list shows 3 programs, find returns correct indices (0 for echo, -1 for unknown), run-by-name invokes correctly with proper RC capture, unknown program prints error and returns -1. Version bumped to v0.3.