Implemented mon_run() program invocation with asm trampoline (mon_invoke_program, svc_exit_impl, svc_set_exit in trampoline.s), prog_start.s for monitor-loaded programs, and three test programs (echo/ret42/exit7). All three return paths verified: normal return rc=0, non-zero RC rc=42, svc_exit non-local return rc=7. Multi-binary justfile build system. Discovered COR24 mov constraints (sp only accepts r0/fp) and worked around them.